#pragma once
#include <GL/glut.h>

namespace Basic {
    int execute(int argc, char *argv[]);
    void drawTringle();
    void drawRectangle();
    void emptyDisplay();
} // namespace Basic
namespace BresenhamLine {
    int execute(int argc, char *argv[]);
}
